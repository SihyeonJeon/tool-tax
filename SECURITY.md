# Security

`tool-tax` reads local JSON files and writes optional reports or packed schema
indexes. It does not execute tool schemas, call MCP servers, use credentials, or
send data over the network.

Report security issues privately through GitHub if a future release changes
that boundary.

