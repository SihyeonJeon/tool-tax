{
  "openapi": "3.1.0",
  "info": {"title": "Demo API", "version": "1.0.0"},
  "paths": {
    "/runs": {
      "post": {
        "operationId": "create_run",
        "summary": "Create a benchmark run",
        "description": "Create a benchmark run with provider, model, prompt variant, task id, raw response, token usage, latency, parse verdict, and score.",
        "parameters": [],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["provider", "model", "task_id", "variant"],
                "properties": {
                  "provider": {"type": "string"},
                  "model": {"type": "string"},
                  "task_id": {"type": "string"},
                  "variant": {"type": "string"},
                  "raw_response": {"type": "string"},
                  "latency_ms": {"type": "integer"},
                  "score": {"type": "number"}
                }
              }
            }
          }
        }
      }
    }
  }
}

